# Clear workspace ---------------------------------------------------------
rm(list = ls())

library("tidyverse")
library("broom")

## PCA (One script, 05_model_i.R --> Output 1 plot)

## K means clustering (One script, 05_model_ii.R --> Output 1 plot)